
<% provide(:title, t(:'new_poll')) %>
<% currently_at t(:'new_poll') %>
<%currently_admin t(:'poll')%>
<%=flash[:notice] = t(:'flash.new_poll') %>
<div class="flex bg-gray-100 ">
  <%= yield :admin_panel %>
  <div class="w-full border-2 px-8">
    <%= render 'form', poll: @poll %>
  </div>

</div>